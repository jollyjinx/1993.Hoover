#
# Generated by the NeXT Project Builder.
#
# NOTE: Do NOT change this file -- Project Builder maintains it.
#
# Put all of your customizations in files called Makefile.preamble
# and Makefile.postamble (both optional), and Makefile will include them.
#

NAME = Fetcher

PROJECTVERSION = 2.6
PROJECT_TYPE = Tool
LANGUAGE = English

CLASSES = Fetcher.m Worker.m HTTPClient.m RobotScanner.m\
          GeneralScanner.m

HFILES = Fetcher.h Worker.h HTTPClient.h nametoaddress.h\
         RobotScanner.h GeneralScanner.h

MFILES = Fetcher_main.m

CFILES = nametoaddress.c

OTHERSRCS = Makefile.preamble Makefile Makefile.postamble\
            HTMLDocument.configuration HTTPClient.configuration


MAKEFILEDIR = $(NEXT_ROOT)/NextDeveloper/Makefiles/pb_makefiles
CODE_GEN_STYLE = DYNAMIC
MAKEFILE = tool.make
LIBS = 
DEBUG_LIBS = $(LIBS)
PROF_LIBS = $(LIBS)


HEADER_PATHS = -I/usr/local/include
FRAMEWORKS = -framework Foundation -framework HooverFramework\
             -framework System


include $(MAKEFILEDIR)/platform.make

-include Makefile.preamble

include $(MAKEFILEDIR)/$(MAKEFILE)

-include Makefile.postamble

-include Makefile.dependencies
